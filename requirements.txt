# smg-metrics — Python dependencies
# Install: pip install -r requirements.txt
# Optional: pip install -r requirements.txt && pip install torch>=2.0.0

muspy>=0.5.0
miditoolkit>=1.0
pretty-midi>=0.2.10
numpy>=1.24
scipy>=1.10
mir-eval>=0.7
tqdm>=0.13.0
